# Scenario Mode is an isolated synthetic runtime. It must never inherit the
# browser-audit authentication shim from the developer's regular .env file.
VITE_AUDIT_AUTH_ENABLED=false
VITE_API_URL=/api/v1
